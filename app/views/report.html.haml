%h2
  %a{href:"/entries/#{group}"}
    =group
-types.each do |entry|
  %a.btn.btn-primary{href:"/entries/#{entry.group}/#{entry.type}"}
    =entry.type
=haml :'partials/lambda_list', locals: {model: model}
